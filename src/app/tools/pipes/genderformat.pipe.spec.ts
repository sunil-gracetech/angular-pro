import { GenderformatPipe } from './genderformat.pipe';

describe('GenderformatPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderformatPipe();
    expect(pipe).toBeTruthy();
  });
});
